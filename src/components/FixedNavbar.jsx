'use client';

import Navbar from './Navbar';

// Wrapper component for fixed navigation
// Converted from Webflow fixed-nav structure
export default function FixedNavbar() {
  return (
    <div className="fixed-nav">
      <Navbar />
    </div>
  );
}
